---
description: Systém případů, průzkumu, rozporů a denního cyklu IN DUBIO
globs: js/cases.js, js/data-loader.js, js/ui.js, data/pool_cases_akt1.json, data/cases-akt1.json, data/cases-akt2.json, data/cases-akt3.json, data/days.json
alwaysApply: false
---

## ZOBRAZENÍ PŘÍPADU — JEDEN SCROLLOVATELNÝ SPIS

Případ NENÍ záložkový systém. Je to jeden scrollovatelný dokument (modál `#modal-pripad`, styly v `css/case-wireframe.css`).

```
HLAVIČKA (typ, název, obžaloba)
SPIS (popis skutku)
SVĚDECTVÍ (karty výpovědí)
PRŮZKUM (akce + zjištění)
ROZSUDEK (dvojkrok: typ → konkrétní varianta, viz `js/ui.js`)
```

**Cíl designu + implementace:** rozsudek je hned k dispozici a sekce rozsudku je při otevření spisu vždy viditelná (včetně pool případů). Průzkum nerozhoduje o tom, zda rozsudek vidíš, ale jen o tom, jak široká sada verdiktů/trestů je právě dostupná.

Hráč scrolluje, čte, volitelně zkoumá, pak soudí.
Po potvrzení rozsudku: předehra v modálu, pak `Cases.zpracujRozsudek` (modal „Důsledky“ se nepoužívá).

## TYPY PŘÍPADŮ

RUTINNÍ: barva #C4A35A | 1 skrytý rozpor | efekty ±2-5
MORÁLNÍ DILEMA: barva #2C4A7C | 2-3 skryté rozpory | efekty ±8-15, vždy Vina nebo Naděje
POLITICKÝ: barva #8B1A1A | 1-2 skryté rozpory + varování | efekty Integrita/MOC ±10-20
OSOBNÍ: barva #5C2D6E | echo Benovy minulosti | efekty Vina ±5-10, Vztahy ±

## DENNÍ STRUKTURA — 20 DNÍ

**Cíl:** 2 případy denně (slot 1 + slot 2). Kapky sdílené mezi oběma.
**Test:** `data/days.json` může dočasně odkazovat na **3** ID z `pool_cases_akt1.json` — před releasem sjednotit na dva sloty.

Pracovní dny: ranní fragment → případy dne → večerní volba.
Sobota: bonusy + shrnutí. Neděle: volba volného dne. Žádné případy.

**Scénář 20 dní (večery, emoční linka / matka, týdenní rytmus):** kanonický textový plán je v `docs/InDubio_20dni_Mapa-scenar.md` (doplněk k tabulce v `docs/InDubio_20dni_Mapa.xlsx`). Při rozporu preferuj tento MD pro vývoj; XLSX drží produkční přehled pro autory.

## HARMONOGRAM VLÁKEN

Božena: D1, 4, 8, 13, 18 (slot 1)
Pospíšil: D3, 7, 14, 16 (slot 2)
Hranice: D5, 10, 17 (slot 2)
Marková: D3, 12, 19 (slot 2)

## SPIS

Hlavička: badge typu, název, obžalovaný, obžaloba, sp.zn., datum.
Popis skutku: 3-5 vět, jazyk soudního protokolu.

## SVĚDECTVÍ

2-3 výpovědi, karty s levým barevným okrajem, každá 2-4 věty.
Výpovědi obsahují skryté rozpory — hra je NIKDY nevysvětluje.

## SYSTÉM ROZPORŮ — HRÁČ JE MUSÍ NAJÍT SÁM

Každý případ má minimálně 1 rozpor ve výpovědích.
Rozpory jsou primárně v textu a hra je nevysvětluje „za hráče".

Výchozí režim (mimo timed pátrání):
- Moudrost ★☆☆☆☆ až ★★★★☆: žádná vizuální nápověda.
- Moudrost ★★★★★ (81-100): možné jemné zvýraznění indicií, bez vysvětlení.

Timed pátrání (pokud je v `clue_system.timed_hunt` zapnuto):
- klikatelné stopy se vizuálně aktivují až po stisku `Spustit pátrání`,
- během odpočtu jsou stopy aktivní, po potvrzení osy jsou uzamčené,
- po potvrzení se barevně zvýrazní jen výsledná (nejlepší potvrzená) vazba.

### Two-Click systém rozporů (Hledání rozporů)

Dobrovolné: hráč může případ ignorovat a rovnou vynést základní rozsudek. Two-Click je vrstva pro hráče, kteří chtějí aktivně číst a odhalit pravdu.

Clues nejsou jen v úvodním textu případu. Mohou být v:
- spisu (`description` / runtime `situation`),
- svědectvích (`testimonies[].text`),
- výstupech průzkumu (`investigation.interview.text`, `records.text`, `informant.variants.*`, `confrontation.success.text`).

V těchto textech nejsou klikací všechna slova. Autoři obalí jen vybrané věty/fráze:
`<span class="clue" data-clue-id="...">...</span>`.

V každém případě je cílově:
- 1 skutečný pár rozporu (`A` + `B`) — správná kombinace.
- ~5 dalších klikatelných frází jako falešné stopy (red herrings).

Pravidlo pro autory dat:
- **jedno `data-clue-id` = jedna unikátní stopa** v rámci případu,
- nepoužívat stejné `data-clue-id` na dvou různých větách v různých blocích textu,
- duplicity vedou k tomu, že se po potvrzení může podbarvit více míst zároveň.

Flow:
1. První klik = výběr stopy (žluté zvýraznění).
2. Druhý klik = pokus o spárování.
3. Neshoda → chyba + reset výběru.
4. Shoda správného páru → zelený flash + potvrzení rozporu.

Po potvrzení správného páru se aktivují až tři odměny:
- A) bonus do ukazatele Informovanost / Průzkum (např. +30 %),
- B) odemčení nové průzkumné akce (za inkoust),
- C) odemčení specifičtějších rozsudků.

Volitelná mini-hra (`timed_hunt`):
- v hlavičce případu je panel pátrání (start / čas / nejlepší vazba / potvrdit),
- bez spuštění pátrání nejsou timed stopy aktivní,
- potvrzení během pátrání bere nejlepší nalezenou vazbu,
- po doběhu času (pokud `auto_confirm_best_on_timeout`) se potvrdí nejlepší nalezená vazba automaticky.

Implementačně:
- párování se vyhodnocuje podle `data-clue-id` a mapy páru v datech případu,
- odměny se vážou na „pair matched“ stav případu,
- UI nesmí hráči předem odhalit, které stopy jsou správné.
- hráči se zobrazuje textové (narativní) hodnocení vazby (např. „průkazná", „dobře podložená"), interně zůstává `weak/medium/strong`.

## SYSTÉM INKOUSTOVÝCH KAPEK

Akce = kapky inkoustu v kalamáři. Vizuální zobrazení na stole.
Kapky SDÍLENÉ mezi oběma případy dne.

### Kapacita
Výchozí: 2 kapky/den. Maximum: 5 kapek/den.

### Jak získat víc kapek
- Neděle procházka/matka: +1 kapka v pondělí
- Odvaha ★★★★★: +1 kapka permanentně
- Večerní volba „pracovat na spisech": +1 kapka zítra, Naděje -2
- Sobotní bonus „pečlivý": +1 kapka příští týden
- Obětovat oběd: +1 kapka DNES, Finance -5, ranní fragment „ruce se třesou" (od Aktu 2)
- Karas důvěra 2+: jednou za hru +1 kapka (rada starého přítele)

### Ceny akcí
- Výslech: 1 kapka
- Záznamy: 1 kapka
- Informátor: 1 kapka
- Konfrontace: 2 kapky

### Strategické napětí
2 případy, 2 kapky. Konfrontace = celý den jednomu případu, druhý naslepo.

## PRŮZKUM — ČTYŘI TYPY AKCÍ

Výsledky se přidávají do spisu jako zelené karty pod svědectvími.

VÝSLECH (1 kapka): motivace. Proč to udělal.
Záznamy (1 kapka): fakta. Tvrdá data, může odkrýt cestu k rozporu.
INFORMÁTOR (1 kapka): kontext. Zabarvený frakcemi (LID/MOC/KAPITÁL).
KONFRONTACE (2 kapky): pravda nebo lež. Riskantní. Podmínky viz níže.

### Konfrontace — podmínky zobrazení (cíl)
1. Hráč nalezl příslušný rozpor (přes záznamy nebo výslech)
2. Odvaha ★★★★☆+ (61+)
**Implementace:** `investigation.confrontation` v `pool_cases_akt1.json` se načte jako čtvrtá položka `hidden_info` (`id: pool_inv_confrontation`, `action: confrontation`, `cost: 2`) v `js/data-loader.js`. Tlačítko v `js/ui.js` strhne **2** akce z `investigationActionsLeft`. Skrytí slotu podle odvahy / odemčení rozporu zatím **není** v engine — doplnit později podle tabulky níže.

Úspěch / neúspěch v JSON (`success` / `failure`) slouží autorům; odhalený text v UI je zatím `prompt` + `success.text` (ne větev podle Odvahy).

### Zamčené sloty — žádné spoilery
Zamčené akce (nedostatečná vlastnost apod.) se zobrazují jako:
  „? — Možnost není k dispozici"
Hráč neví CO ani PROČ. Ví jen, že mu něco chybí.
Konfrontace se nezobrazuje VŮBEC dokud nejsou splněny obě podmínky.

## ROZSUDEK

**Cíl:** tlačítko potvrzení viditelné hned; hráč může soudit naslepo.
**Implementace (`js/ui.js`):** sekce rozsudku (`#case-wf-sec-verdict`) je **vždy viditelná** při otevřeném spisu. Nabídka verdiktů se filtruje podle počtu odhalených položek `hidden_info` u **pool** případů (`_wfFiltrovatVerdiktyPodlePruzkumu` po `_wfFiltrovatVerdikty`): při **0 odhaleních** jen Vinen (max./přiměřený) + Nevinen (formální); **1+** přidá mírnou vinu a zproštění s komentářem; **2+** i nedostatek důkazů (dál omezuje Moudrost v datech); **alternativní trest** po odhalené konfrontaci nebo při **≥3** odhaleních; **speciální** jen po konfrontaci, pokud v JSON existuje. Legacy případy (`_fromPool` nepravda) — plná sada po rysech. Po každém průzkumu se wireframe verdiktů znovu vykreslí (`_wfPoPrvniAkciPruzkumu`). Lišta **Informovanost** (`#case-wf-inform-wrap`) podle odhalení + konfrontace = 100 %.

### Informovanost (progress bar)
0 akcí = 15% šedá „naslepo"
1 akce = 40% žlutá „základní"
2 akce = 70% zelená „důkladný"
2 akce + rozpor = 90% zelená „pečlivý"
konfrontace = 100% zelená „kompletní"

### Průzkum odemyká možnosti rozsudku

| Průzkum | Viditelné verdikty a tresty |
|---------|----------------------------|
| 0 akcí | Vinen (maximální, přiměřený) + Nevinen (formální) |
| 1 akce | + Vinen (mírný) + Nevinen (s komentářem) |
| 2 akce | + Nedostatek důkazů (Moudrost 3+) + Zproštěn (Integrita 3+) |
| 2 akce + rozpor | + Alternativní trest (Moudrost 4+) |
| Konfrontace úspěšná | + Speciální možnost specifická pro případ |

Zamčené verdikty/tresty = „?" sloty. Hráč neví co mu chybí.

### Vizuální zvýraznění odemčených možností

Rozsudky a tresty odemčené průzkumem (spotřeba akcí) nebo potvrzenou dvojicí stop musí být vizuálně odlišené od základních možností.

UI pravidlo:
- odemčená možnost má odlišnou barvu celého tlačítka (ne jen štítek),
- text `Průzkum:` je přímo v názvu odemčené volby, např.:
  `Průzkum: Pokuta 50 Kčs, důtka`.

Skupinová tlačítka (Krok 1):
- pokud skupina obsahuje nové odemčené volby, zobrazit pod titulkem text:
  - `Průzkum: +1 nový rozsudek`
  - `Průzkum: +2 nové rozsudky`
  - `Průzkum: +X nových rozsudků` (pro vyšší počty)

Příklad:
- `Vinen` a `Zproštění / nevinen` mohou zůstat základní,
- `Nedostatek důkazů` se barevně zvýrazní až po odemčení průzkumem.

Cíl:
- hráč okamžitě vidí, co je základ a co už objevil aktivním průzkumem,
- odemykání skrytých verdiktů a trestů je atraktivní a motivační.

### Dvoudílný rozsudek
Krok 1 — VERDIKT: Vinen / Nevinen / Nedostatek důkazů / Zproštěn
Krok 2 — TREST (objeví se po výběru verdiktu)

Pokud VINEN:
- Maximální — zmizí při Vina ★★★★★
- Přiměřený — vždy
- Mírný — průzkum 1+
- Alternativní — průzkum 2+, rozpor, Moudrost 4+
- Speciální — po konfrontaci

Pokud NEVINEN: Formální (vždy) / S komentářem (průzkum 1+)
Pokud ZPROŠTĚN: jeden přístup
Pokud NEDOSTATEK DŮKAZŮ: Vrátit k došetření / Uzavřít

Po výběru obou kroků: „Potvrdit rozsudek" (nenávratné).

## KVALITA ROZSUDKU

| Průzkum | Soulad | Zpětná vazba |
|---------|--------|-------------|
| 0 akcí | — | „Rozhodl jsi naslepo." Finance +5, Moudrost -2 |
| 1 akce | ano | „Rozhodl jsi s tím co jsi měl." Finance +10 |
| 1 akce | ne | „Věděl jsi víc — a přesto..." Vina +3 |
| 2+ akce | ano | „Případ jsi prostudoval pečlivě." Finance +20, Moudrost +3 |
| 2+ akce | ne | „Viděl jsi pravdu. Odvrátil ses." Vina +5, Integrita -3 |

### Procesní kvalita + normativní směr (meta vrstva nad JSON body)

Pevné body v `verdicts.*.effects` zůstávají **base vrstva**.
Nad nimi běží meta-vrstva vyhodnocení:

1. **Procesní kvalita** (`nizka` / `stredni` / `vysoka`)
   - vstupy: informovanost, odhalené informace, potvrzená vazba, soulad s průzkumem,
   - dopad primárně: `Moudrost`, `Vina`, `Integrita`.

2. **Normativní směr rozsudku** (`legalistni` / `vyvazeny` / `socialni`)
   - vstupy: charakter verdiktu a jeho dopady na frakce,
   - dopad primárně: `Moc`, `Lid`, `Kapital`.

Pravidlo vyvážení:
- base JSON efekty = cca 70–85 % výsledku,
- meta vrstva = cca 15–30 % výsledku.

### Oddělení průzkumu vs. pátrání (kanon)

- **Průzkum** = faktická opora rozhodnutí (evidence).
- **Pátrání (Two-Click)** = koherence osy stop (coherence) + odemykání některých možností.
- Pátrání samo o sobě nenahrazuje faktický soulad s průzkumem; obě osy se vyhodnocují zvlášť.

### Skryté verdikty — povinná motivace

Verdikty odemčené průzkumem/pátráním musí být systémově atraktivní:

- baseline pravidlo: numerické důsledky odemčeného verdiktu dostanou fixní koeficient (aktuálně 1.25x),
- volitelně může mít takový verdikt i unikátní efekt (`flags`, delayed trigger), ale koeficient je povinný základ.

## DOPAD PO ROZSUDKU

**Typ případu (kód):** po uzavření spisu přidá `Cases.vypoctiTypoveDoplnky` k finance typový příplatek — **morální +25 Kčs**, **politický +35 Kčs**, **osobní +30 Kčs** (rutinní bez tohoto řádku). U **morální / politický / osobní** navíc `_bonusInkoustZaNarocnySpis`: **+1** `investigationActionsLeft`. Souhrn pro hráče také v `data/knihovna.json` a `.cursor/rules/economy.mdc`.

Spis zmizí. Nahradí ho:
1. Razítko VYŘEŠENO (červená = odsouzení, modrá = zproštění, šedá = odloženo)
2. Narativní fragment 2-3 věty. Nikdy nehodnotí hráče.
3. Shrnutí informovanosti (jednovětý)

Tón se mění podle verdiktu, Viny, Naděje.
Mechanické dopady skryté. Odložené důsledky za X dní.

## VÁHOVÁNÍ PŘÍPADŮ (20 dní)

D1-3: rutinní 100%
D4-7: rutinní 60%, morální 40%
D8-12: rutinní 30%, morální 35%, politický 25%, osobní 10%
D13-20: rutinní 15%, morální 30%, politický 35%, osobní 20%

Dynamické: Integrita >70 více morálních | <30 více úplatků
LID <30 více chudých | Odvaha >70 více politických | Vina >70 echo staré chyby

## TÝDENNÍ CYKLUS

Neděle:
A) Matka → Naděje +5, Vina -3, Finance -5, +1 kapka pondělí
B) Kostel → Vina -2, Naděje +2
C) Spisy → Moudrost +3, Naděje -2, extra detail
D) Procházka → Naděje +3, Moudrost +2, +1 kapka pondělí
E) Nepsat synovi → Vina +3

Večerní volby:
- Psát Martinovi → Vina -2
- Pracovat na spisech → +1 kapka zítra, Naděje -2
- Procházka → Naděje +2
- Hospoda → náhodné setkání
- Obětovat oběd → +1 kapka DNES, Finance -5 (od Aktu 2)

Sobotní bonusy:
Pečlivý (75%+ průzkum): Moudrost +5, Finance +15, +1 kapka příští týden
Nezávislý (bez úplatku): Integrita +5, LID +8
Odvážný (žádný odložen): Odvaha +3, Finance +20
Konzistentní (5+ stejným směrem): MOC +10 nebo LID +10

## KRIZOVÉ STAVY — DOPADY NA PŘÍPADY

### Integrita pod 10
- ZMIZÍ verdikty: zproštění obžaloby, alternativní tresty, komentáře
- PŘIBUDE: volba „Přijmout úplatek" u každého případu (skryté +50-100 Kčs, Integrita -15)
- Zamčené „?" sloty u odstraněných verdiktů se NEZOBRAZUJÍ (zmizí úplně, jako by neexistovaly)

### Odvaha pod 10
- ZMIZÍ: konfrontace (i odemčená), veřejná prohlášení
- PŘIBUDE: zvýrazněná volba „Odložit případ" u politických případů
- Po 3 odloženích za sebou: Vlčkův dopis (automatický, ne hráčova volba)
- Zamčené „?" sloty u konfrontace se NEZOBRAZUJÍ

### Finance = 0
- Haasova obálka svítí zlatě na stole (vizuální prvek, ne popup)
- Hráč ji může otevřít KDYKOLI — ne jen na den 16
- Efekty viz economy.mdc

## ENCYKLOPEDIE — HERNÍ WIKIPEDIE

Klíčová slova v textech případů, dopisů a fragmentů mají prokliky na encyklopedická hesla.

### Vizuální označení
Slova s heslem mají jemné plné podtržení v tlumené modré barvě (#85B7EB). ODLIŠNÉ od:
- Rozporů (žádné zvýraznění, max čárkované podtržení při Moudrosti 5 hvězd)
- Běžného textu (žádné podtržení)

### Data
Soubor: data/encyclopedia.json
Struktura: { "heslo_id": { "title": "...", "text": "3-5 vět", "related": ["dalsi_heslo"] } }
Celkem cca 30-40 hesel, psáno na konci vývoje.

### Kategorie hesel
SVĚT: ČSR, Sudety, Masaryk, Beneš (prezident), hospodářská krize, Creditanstalt, NSDAP, Piłsudski
SPOLEČNOST: žebračenky, pochody hladu, NOF, DNSAP, exekuce, Baťa, české fašistické hnutí, Gajda
JUSTICE: krajský soud, trestní zákon 1852, zákon na ochranu republiky č.50/1923, porotní soud

## PRAVIDLA PRO PSANÍ PŘÍPADŮ

1. Každý případ má alespoň 1 rozpor — i rutinní.
2. Výpovědi max 4 věty.
3. Rozpor subtilní — hráč najde čtením, ne zvýrazněním.
4. Rozpor má důsledek pro verdikt.
5. Každá akce změní pohled na případ.
6. Konfrontace = dramatická mini-scéna.
7. Morální/politické nemají správnou odpověď.
8. Aftermath nikdy nehodnotí.
9. Odložené důsledky = konkrétní obraz.
10. Zamčené sloty NIKDY neprozrazují obsah — jen „?" a „Možnost není k dispozici."
11. Případy musí být ZASAZENÉ V ČESKOSLOVENSKU 1931 — konkrétní reálie, instituce, zákony, místa.
12. Používat skutečné právní předpisy (trestní zákon 1852, zákon na ochranu republiky č.50/1923).
13. Zmiňovat dobové instituce (zemský úřad, živnostenský rejstřík, Pojišťovna Slavia, Creditanstalt).
14. Jména a místa česká/moravská/sudetská — Národní třída, Staroměstské náměstí, Žižkov, Smíchov.
15. Klíčová slova s encyklopedickým heslem označit v JSON: encyclopedia_links: ["heslo_id1", "heslo_id2"].
16. Každý případ by měl letmo odrážet dobu — chudoba, strach, politické napětí, národnostní třecí plochy.

## JSON — `data/pool_cases_akt1.json` (zdroj) → runtime (`normalizujPoolPripad` v `js/data-loader.js`)

Kořen: `{ "pool_cases_akt1": [ { …případ… }, … ] }`.

### Společná metadata (zdrojové pole, část přepíše normalizace)

| Pole ve JSON | Poznámka |
|--------------|----------|
| `id` | Trvalé ID případu |
| `slot`, `act`, `color`, `weight_conditions`, `available_days` | Metadata / váhy (typ dne řeší `cases.js` + `days.json`) |
| `type` v JSON může být `"routine"` — **loader nastaví** `type: "rutinni"` pro UI |
| `title`, `case_number`, `defendant`, `charge` | Hlavička + spis |
| `description` | → runtime `situation` (text „skutku“) |
| `contradictions` | Autorům + text rozporu v modalu (`pripad.contradictions`); `highlight_at_wisdom` / `contradiction_keywords` zatím **neřeší** engine |
| `aftermath`, `delayed_consequences` | Texty / triggery pro budoucí dopady (ne vše je nutně napojeno) |
| `correct_verdict` | **Nepoužívá se** v herní logice (legacy v některých `cases-akt*.json`). Úklis: `node tools/patch-case-bonus-data.js` pole maže. |

### `testimonies[]` (JSON) → `testimony[]` (runtime)

Loader mapuje jen **`speaker` → `label` / `source`** a **`text`**. Pole `id`, `contradiction_keywords`, `hidden_at_wisdom_below` zůstávají ve zdroji pro autory / budoucí UI podtržení podle Moudrosti.

### `investigation` → `hidden_info[]` + tlačítka průzkumu

| Klíč | `hidden_info[].id` | `action` | `reveal` | `cost` |
|------|---------------------|----------|----------|--------|
| `interview` | `pool_inv_interview` | `witness` | `interview.text` | 1 |
| `records` | `pool_inv_records` | `records` | `(source)\\n\\n` + `text` | 1 |
| `informant` | `pool_inv_informant` | `informant` | výběr z `variants` dle frakcí (`moc_high` ≥60, `lid_high` ≥60, `kapital_high` ≥60, jinak `default`) | 1 |
| `confrontation` | `pool_inv_confrontation` | `confrontation` | `prompt` + `success.text` | **2** |

**Neoficiální zdroj (UI, dříve „nečistá cesta“):** u položek `hidden_info` kromě **konfrontace** lze vedle oficiálního tlačítka průzkumu (kapky) zobrazit druhé tlačítko **„Neoficiální zdroj“** — stejné `reveal` jako průzkum, bez kapek, za postih (výchozě finance cca −12 / −20 Kčs podle `cost`, Integrita −2, Vina +1; override v JSON). Konfrontace je **výhradně** za akční body (inkoust), druhá cesta u ní **neexistuje**. Vypnutí: `dirty_unlock: false`. Data pole zůstává `dirty_unlock` v `pool_cases_*.json` → loader přenáší do `hidden_info[].dirty_unlock`. Při bezplatných záznamech se druhá cesta neukazuje. Moudrost po neoficiálním zdroji roste slaběji (násobek 2 oproti 3). Karta zjištění ve spisu má jiný štítek než tlačítko: např. „Zjištění — výslech (průzkum)“ vs. „… (neoficiální zdroj)“; způsob se ukládá ve stavu (`revealedInfoPath`).

**Informovanost — tooltip:** lišta má shrnutí, že kromě odemykání části verdiktů vstupuje do **procesní kvality** a důsledků po rozsudku (metavrstva).

**Verdikty — tooltip (krok 2):** u každé dostupné varianty krátký text, *proč* je zpřístupněná (základ / počet průzkumů / konfrontace / práh informovanosti / odměna z pátrání). U zamčených variant důvod blokace (rysy / průzkum / práh / pátrání). Implementace: `Cases.popisDuvoduVerdiktu`, `Cases.popisNepristupnostiVerdiktu` v `js/cases.js`, vykreslení v `js/ui.js`.

**Slabý podklad (UI):** při nízké informovanosti drobné upozornění **jen u tlačítek konkrétních verdiktů v kroku 2** — ne u výběru skupiny v kroku 1, ne u legacy seznamu. `_wfHtmlUpozorneniSlabySpis` v `js/ui.js`.

**Předběžný hint dopadů (krok 2):** finance číslem v **Kčs**; traits / důvěra jako osy bez čísel; **frakce** jedním řádkem `Frakce: A, B` (deduplikace názvů). `_wfVerdiktDopadovyHint` v `js/ui.js`.

**Archiv Rozsudky:** filtr podle typu případu, štítek na řádku, uložené **`caseType`** v záznamu (`State.pridejRozsudek`). Starší save bez `caseType` dopočítá typ ze spisu. `js/ui.js` (`_vyplnArchivTab`). Pod filtrem krátký blok **trendů** (směr výroků, uložená procesní / normativní meta kde je) — bez hodnocení „správnosti“ rozhodnutí.

**Pátrání vs. prah informovanosti (verdikty):** je-li verdikt v `info_threshold_unlocks.verdicts` s práhem `min`, považuje se za odemčený, pokud platí **informovanost ≥ min NEBO** je ID v `unlock_verdict_ids` u odměny za **již potvrzenou** osu stop (`jeVerdiktOdemcenPoClue` — varianta B). Jinak prah zůstává závazný jen jako alternativa k odměně z pátrání.

Volitelná metadata u konfrontace v JSON (`requires_contradiction`, `requires_courage`, `failure`) — dokumentace pro scénář; **filtrace podle Odvahy zatím v UI není**.

### `verdicts` → pole `verdicts[]` s `id` pro dvojkrok UI

Funkce `_slozitPoolVerdikty` skládá ploché položky:

- **Vinen:** `guilty.sentences.<klíč>` → `id: "guilty_<klíč>"` (např. `guilty_standard`), `text` = `label`, `consequence` = `description`, `consequences` z `effects` (malá písmena → rysy/frakce/finance přes `_poolEffectsNaConsequences`).
  - `requires.moudrost_min` → `requires_moudrost_min`
  - `requires.odvaha_min` → `requires_odvaha_min`
  - `available_unless.vina_above` → `available_unless_vina_above`
- **Zproštění / nevinen:** `not_guilty.approaches.<klíč>` → `id: "not_guilty_<klíč>"` (v JSON např. `with_comment`, ne `comment`).
- **Nedostatek důkazů:** `insufficient_evidence` + každá položka `options` → `id: "insufficient_<klíč>"`, společné `requires.moudrost_min` na `insufficient_evidence`.

**Filtr ve hře:** `_wfFiltrovatVerdikty` v `js/ui.js` skrývá položku, pokud nesplní `requires_moudrost_min`, `requires_odvaha_min` nebo `available_unless_vina_above` (aktuální stav rysů).

Klíče `guilty.sentences` odpovídají souborům v poolu typicky: `maximum`, `standard`, `lenient`, `alternative` (volitelně další, např. `special`, pokud loader najde objekt).

### `clue_system` (Two-Click rozpor)

Volitelná větev pro interaktivní párování stop v textu (`<span class="clue" data-clue-id="...">`):

| Pole | Význam |
|------|--------|
| `enabled` | Zapnutí mechaniky pro případ |
| `true_pair_id` | ID skutečného rozporu (v tomto designu 1 pár) |
| `pairs[]` | Seznam párů: `{ pair_id, a_id, b_id, strength }` |
| `decoys[]` | Pole falešných klikatelných stop (`data-clue-id`) |
| `rewards.on_confirm.<strength>.investigation_progress_delta` | Bonus do informovanosti po potvrzení osy |
| `rewards.on_confirm.<strength>.unlock_actions[]` | ID průzkumných akcí odemčených potvrzenou osou |
| `rewards.on_confirm.<strength>.unlock_verdict_ids[]` | ID verdiktů odemčených potvrzenou osou |
| `rewards.on_confirm.<strength>.narrative_lines[]` | Volitelné: krátké odstavce (typicky **3 věty** podle síly vazby); po potvrzení osy se zobrazí modal v `#modal-pripad`, tlačítko **Rozumím** dokončí vykreslení potvrzení ve spisu (`js/ui.js`). |
| `rewards.on_confirm.<strength>.narrative` | Alternativa k `narrative_lines`: jeden řetězec, odstavce oddělte `\n\n`. |
| `info_threshold_unlocks.actions[]` | Prahové odemykání průzkumných akcí podle informovanosti |
| `info_threshold_unlocks.verdicts[]` | Prahové odemykání verdiktů podle informovanosti |
| `timed_hunt.enabled` | Zapnutí mini-hry pátrání |
| `timed_hunt.duration_sec` | Délka odpočtu (doporučeno 45–90 s) |
| `timed_hunt.speed_upgrade_sec` | Hranice pro rychlostní bonus při potvrzení |
| `timed_hunt.auto_confirm_best_on_timeout` | Po doběhu času potvrdit nejlepší nalezenou vazbu |
| `timed_hunt.timeout_downgrade` | Volitelné oslabení síly po timeoutu |

Pravidlo párování:
- hráč musí kliknout dvě odlišné stopy,
- shoda = obě stopy patří do stejného `pair_id`,
- neshoda = reset výběru bez odemčení odměn.

Pozn.: `span.clue` může být v libovolném textovém poli, které UI renderuje v detailu případu (spis, svědectví, odhalené průzkumné výstupy). Cíl je, aby hráč hledal stopu aktivně i po použití Průzkumu.

## KUCHAŘKA AUTORA PŘÍPADU (CHECKLIST)

Použij tento postup pro každý nový případ:

1. **Jádro případu**
   - definuj obžalobu, minimálně jeden skrytý rozpor, hlavní morální napětí.

2. **Textové vrstvy**
   - `description` (spis), `testimonies[]`, `investigation.*.text`,
   - clues rozprostři napříč vrstvami (nejen v úvodu).

3. **Clue mapa**
   - urči 1 silný pár (`strong`) + 1–2 slabší relevantní vazby (`medium/weak`) + red herrings,
   - zkontroluj unikátnost `data-clue-id` (žádné duplicity stejného ID na různých větách).

4. **Odměny a odemykání**
   - nastav `rewards.on_confirm` po síle vazby,
   - u každé sily (`weak` / `medium` / `strong`) zvaž **`narrative_lines`**: tři krátké věty v barvě „zápis z pátrání“ — od náznaku přes střední jistotu po pevné spojení faktů (bez prozrazení správného páru mimo tento moment),
   - nastav `info_threshold_unlocks` pro akce a verdikty.

5. **Timed pátrání (volitelně)**
   - nastav `timed_hunt` parametry,
   - ověř, že bez startu pátrání nejsou timed stopy aktivní.

6. **Rozsudky**
   - připrav plnou škálu (`guilty`, `not_guilty`, `insufficient_evidence`) a varianty,
   - každé variantě dej smysluplné `effects` (base vrstva),
   - ke každému **viditelnému** textu verdiktu (`label` / `description` / `consequence`) přidej větu **lidského dopadu** — co to znamená pro obžalovaného, poškozeného nebo rodinu (ne jen paragraf nebo částku),
   - ověř, co je základní a co je odemykané průzkumem/vazbami.

7. **Meta logika hry**
   - případ musí podporovat jak „právní", tak „morální" interpretaci,
   - rozsudek má dávat smysl při nízké i vysoké procesní kvalitě (jiný risk, jiný dopad).

8. **Aftermath + delayed**
   - napiš aftermath bez hodnocení hráče,
   - doplň odložený důsledek pro aspoň jednu klíčovou větev.

9. **Technická kontrola**
   - validní JSON,
   - neexistující `clue-id` v párech/decoys,
   - konzistence `unlock_*` ID vůči reálným `hidden_info` a `verdicts`.

### Legacy případy (`cases-akt*.json`)

Struktura se může lišit; kanon pro nový obsah je **pool** výše. Při rozšíření loaderu nebo legacy držet shodné runtime pole: `testimony`, `hidden_info`, `verdicts`, `situation`.

## Krátká revize spisu (odvolání) — autorská mini šablona

Když se případ vrátí k revizi, hra zobrazí krátkou kartu (A/B volba), ne full replay.

**Univerzální dopis na stole (jedna šablona pro všechny revize):** obálka / lístek z úřadu nebo z advokacie má společný rámec („věc se vrací k přezkumu“), konkrétní jméno a číslo spisu se doplní z dat případu. Autor případu nepíše nový typ dopisu pro každý JSON — jen `review_card.*` a případné unikátní efekty. Viz také `docs/spis-patrani-pruzkum-rozsudek.md`.

Doporučená data v případu:

- `review_card.summary_short` — 2–3 věty, co se po pár dnech otevřelo.
- `review_card.option_a_text` — stručný text volby „podržet rozsudek“.
- `review_card.option_b_text` — stručný text volby „zmírnit/revidovat“.
- `review_card.unique_effect_a` / `review_card.unique_effect_b` (volitelné) — extra dopad navíc k systémovým efektům revize.

Pokud `review_card` chybí, engine použije generický fallback z názvu případu a původního verdiktu.
